{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "migrate": "php artisan migrate:fresh --seed",
        "serve": "php artisan serve",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
